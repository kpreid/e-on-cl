# Copyright 2005 Kevin Reid, under the terms of the MIT X license
# found at http://www.opensource.org/licenses/mit-license.html ................

  ? print("hi")
  # stdout: hi
  
  ? println("hi")
  # stdout: hi
  #         
  
  ? if (E.toQuote(updoc) !~ `<updoc runner for <file:@{_}updoc.updoc>>`) { [updoc] }

This should be successful:
  
  ? updoc.push(e`1 + 1`, [["value", "2"]])

This should fail:

  ? updoc.push(e`1 + 1`, [["value", "You should see a failure report, expecting the value 2."]])


  ? "something after pushed tests"
  # value: "something after pushed tests"